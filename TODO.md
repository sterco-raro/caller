# TODO

- Add support for manual connections, like `caller connect [--private-key ~/.ssh/key] [--user root] [--name some-name] <host or ip address>`
- Add support for saving a new connection?
  - "caller create ..."
  - or maybe asking to save the new connection after a manual caller connect
